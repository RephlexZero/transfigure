[package]
name = "og-gen"
version.workspace = true
edition.workspace = true
license-file.workspace = true

[[bin]]
name = "og-gen"
path = "src/main.rs"

[dependencies]
# Default features include "text" — needed for font-aware SVG text rendering.
resvg = "0.44"
